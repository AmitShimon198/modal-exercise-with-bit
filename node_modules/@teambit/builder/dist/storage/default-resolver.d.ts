import { Component } from '@teambit/component';
import { StorageResolver } from './storage-resolver';
import type { ArtifactList } from '../artifact';
export declare class DefaultResolver implements StorageResolver {
    name: string;
    store(component: Component, artifactList: ArtifactList): Promise<void>;
}
