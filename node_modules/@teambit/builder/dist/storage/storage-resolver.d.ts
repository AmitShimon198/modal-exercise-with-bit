import { Component } from '@teambit/component';
import { ArtifactList } from '../artifact';
export declare type StoreResult = {
    [path: string]: string;
};
export interface StorageResolver {
    name: string;
    store(component: Component, artifacts: ArtifactList): Promise<void>;
}
