import { Component } from '@teambit/component';
import { ExecutionContext } from '@teambit/envs';
import { PubsubMain } from '@teambit/pubsub';
import { DevServer } from './dev-server';
export declare class ComponentServer {
    private pubsub;
    readonly context: ExecutionContext;
    readonly portRange: number[];
    readonly devServer: DevServer;
    errors?: Error[];
    constructor(pubsub: PubsubMain, context: ExecutionContext, portRange: number[], devServer: DevServer);
    hostname: string | undefined;
    hasComponent(component: Component): Component | undefined;
    get port(): number;
    _port: number;
    listen(): Promise<void>;
    private getHostname;
    private onChange;
    private createComponentsServerStartedEvent;
    get url(): string;
}
