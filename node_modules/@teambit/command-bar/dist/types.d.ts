export declare type CommandId = string;
export declare type CommandHandler = (...arg: any[]) => any;
export declare type CommanderSearchResult = {
    id: string;
    displayName: string;
    handler: CommandHandler;
    icon?: string;
    iconAlt?: string;
    keybinding?: Keybinding;
};
export interface SearchProvider {
    search(term: string, limit: number): CommanderSearchResult[];
    test(term: string): boolean;
}
export declare type Keybinding = string | string[];
