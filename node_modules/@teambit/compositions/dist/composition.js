"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Composition = void 0;

function _compositionsModel() {
  const data = require("@teambit/compositions.model.composition-id");

  _compositionsModel = function () {
    return data;
  };

  return data;
}

class Composition {
  constructor(
  /**
   * identifier of the composition
   */
  identifier,
  /**
   * file path in which the composition is contained.
   */
  filepath) {
    this.identifier = identifier;
    this.filepath = filepath;
  }

  get displayName() {
    return (0, _compositionsModel().humanizeCompositionId)(this.identifier);
  }

  toObject() {
    return {
      identifier: this.identifier,
      filepath: this.filepath
    };
  }

  static fromArray(compositions) {
    return compositions.map(composition => {
      return new Composition(composition.identifier, composition.filepath);
    });
  }

}

exports.Composition = Composition;

//# sourceMappingURL=composition.js.map