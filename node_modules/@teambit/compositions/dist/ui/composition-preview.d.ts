import { ComponentModel } from '@teambit/component';
import { Composition } from '../composition';
export declare type ComponentCompositionProps = {
    className?: string;
    component: ComponentModel;
    composition?: Composition;
    queryParams?: string | string[];
    hotReload?: boolean;
};
export declare function ComponentComposition({ component, composition, hotReload, className, queryParams, }: ComponentCompositionProps): JSX.Element;
export declare namespace ComponentComposition {
    var defaultProps: {
        skipHotReload: boolean;
    };
}
