import { Registry } from './registry';
export declare class Registries {
    readonly defaultRegistry: Registry;
    readonly scopes: Record<string, Registry>;
    constructor(defaultRegistry: Registry, scopes: Record<string, Registry>);
    setDefaultRegistry(registry: Registry): Registries;
    updateScopedRegistry(name: string, registry: Registry): Registries;
}
