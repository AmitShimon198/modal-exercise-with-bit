"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.DocPropList = void 0;

function _docProp() {
  const data = require("./doc-prop");

  _docProp = function () {
    return data;
  };

  return data;
}

class DocPropList {
  constructor(docProps) {
    this.docProps = docProps;
  }

  get(name) {
    return this.docProps.find(docProp => docProp.name === name);
  }

  static from(object) {
    const props = Object.keys(object).map(key => {
      if (!object[key]) return undefined;
      return new (_docProp().DocProp)(key, object[key]);
    }).filter(prop => !!prop);
    return new DocPropList(props);
  }

}

exports.DocPropList = DocPropList;

//# sourceMappingURL=doc-prop-list.js.map