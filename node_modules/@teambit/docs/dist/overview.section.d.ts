import { Section } from '@teambit/component';
import { DocsUI } from './docs.ui.runtime';
export declare class OverviewSection implements Section {
    private docs;
    constructor(docs: DocsUI);
    navigationLink: {
        href: string;
        exact: boolean;
        children: string;
    };
    route: {
        path: string;
        exact: boolean;
        children: JSX.Element;
    };
    order: number;
}
