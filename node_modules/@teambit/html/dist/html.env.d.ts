import { Environment } from '@teambit/envs';
export declare class HtmlEnv implements Environment {
    icon: string;
    getDocsTemplate(): string;
    getMounter(): string;
    getDependencies(): {
        devDependencies: {
            '@types/jest': string;
        };
    };
}
