import { ImportOptions } from '../../../consumer/component-ops/import-components';
export declare type EnvironmentOptions = {
    tester: boolean;
    compiler: boolean;
};
export default function importAction(environmentOptions: EnvironmentOptions, importOptions: ImportOptions, packageManagerArgs: string[]): Promise<any>;
