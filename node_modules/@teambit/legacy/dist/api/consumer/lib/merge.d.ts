import { ApplyVersionResults, MergeStrategy } from '../../../consumer/versions-ops/merge-version';
export default function merge(values: string[], mergeStrategy: MergeStrategy, abort: boolean, resolve: boolean, noSnap: boolean, message: string, build: boolean): Promise<ApplyVersionResults>;
