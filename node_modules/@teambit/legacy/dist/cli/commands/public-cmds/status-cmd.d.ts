import { StatusResult } from '../../../api/consumer/lib/status';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
import { Group } from '../../command-groups';
export declare const statusFailureMsg = "issues found";
export declare const statusInvalidComponentsMsg = "invalid components";
export declare const statusWorkspaceIsCleanMsg = "nothing to tag or export (use \"bit add <file...>\" to track files or directories as components)";
export declare const individualFilesDesc = "these components were added as individual files and not as directories, which are invalid in Harmony\nplease make sure each component has its own directory and re-add it. alternatively, use \"bit move --component\" to help with the move.";
export default class Status implements LegacyCommand {
    name: string;
    shortDescription: string;
    group: Group;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    json: boolean;
    action([]: Iterable<any>, { json, strict }: {
        json?: boolean;
        strict?: boolean;
    }): Promise<any>;
    report({ newComponents, modifiedComponent, stagedComponents, componentsWithIssues, importPendingComponents, autoTagPendingComponents, invalidComponents, outdatedComponents, mergePendingComponents, componentsDuringMergeState, componentsWithIndividualFiles, componentsWithTrackDirs, softTaggedComponents, snappedComponents, }: StatusResult): string;
}
