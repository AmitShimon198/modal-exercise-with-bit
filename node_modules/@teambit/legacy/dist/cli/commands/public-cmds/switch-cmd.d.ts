import { ApplyVersionResults, MergeStrategy } from '../../../consumer/versions-ops/merge-version';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class Switch implements LegacyCommand {
    name: string;
    description: string;
    private: boolean;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    action([lane]: [string], { remote, as, merge, getAll, verbose, json, }: {
        remote?: string;
        as?: string;
        merge?: MergeStrategy;
        getAll?: boolean;
        verbose?: boolean;
        override?: boolean;
        json?: boolean;
    }): Promise<ApplyVersionResults>;
    report({ components, failedComponents, lane, json, }: {
        components: ApplyVersionResults['components'];
        failedComponents: ApplyVersionResults['failedComponents'];
        lane: string;
        json: boolean;
    }): string;
}
