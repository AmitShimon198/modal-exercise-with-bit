import { BitId, BitIds } from '../../bit-id';
export default class WorkspaceLane {
    lanePath: string;
    laneName: string;
    ids: BitIds;
    constructor(lanePath: string, laneName: string, ids: BitIds);
    addEntry(componentId: BitId, replaceIgnoreVersion?: boolean): void;
    removeEntry(componentId: BitId): void;
    reset(): void;
    getIds(): BitIds;
    static load(laneName: string, scopePath: string): WorkspaceLane;
    write(): Promise<void>;
}
