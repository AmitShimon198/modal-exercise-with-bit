"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ParentDirTracked = void 0;

function _bitError() {
  const data = require("@teambit/bit-error");

  _bitError = function () {
    return data;
  };

  return data;
}

class ParentDirTracked extends _bitError().BitError {
  constructor(parentDir, componentName, currentDir) {
    super(`unable to add "${currentDir}", an existing component "${componentName}" already has its parent-dir "${parentDir}" as the root-dir.
if you would like "${currentDir}" as a separate component, please extract it outside "${parentDir}"`);
  }

}

exports.ParentDirTracked = ParentDirTracked;