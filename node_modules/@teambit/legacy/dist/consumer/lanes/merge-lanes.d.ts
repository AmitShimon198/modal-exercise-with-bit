import { Consumer } from '..';
import { ApplyVersionResults, MergeStrategy } from '../versions-ops/merge-version';
export declare function mergeLanes({ consumer, mergeStrategy, laneName, remoteName, noSnap, snapMessage, existingOnWorkspaceOnly, build, }: {
    consumer: Consumer;
    mergeStrategy: MergeStrategy;
    laneName: string;
    remoteName: string | null;
    noSnap: boolean;
    snapMessage: string;
    existingOnWorkspaceOnly: boolean;
    build: boolean;
}): Promise<ApplyVersionResults>;
