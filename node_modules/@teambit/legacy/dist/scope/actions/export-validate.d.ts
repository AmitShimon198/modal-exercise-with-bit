import { Scope } from '..';
import { Action } from './action';
declare type Options = {
    clientId: string;
    isResumingExport: boolean;
};
export declare class ExportValidate implements Action<Options> {
    scope: Scope;
    clientId: string;
    execute(scope: Scope, options: Options): Promise<void>;
    private waitIfNeeded;
    private getPendingDir;
    private sleep;
    private clientIdExistsInPendingDir;
    private getClientsQueue;
    private getClientIdsDirs;
}
export {};
