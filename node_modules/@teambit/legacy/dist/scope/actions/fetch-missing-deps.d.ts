import { Scope } from '..';
import { Action } from './action';
declare type Options = {
    ids: string[];
    fetchFromOriginalScopes: boolean;
};
export declare class FetchMissingDeps implements Action<Options> {
    execute(scope: Scope, options: Options): Promise<void>;
}
export {};
