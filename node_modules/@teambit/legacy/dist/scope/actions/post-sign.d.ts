import { Scope } from '..';
import { Lane } from '../models';
import { AuthData } from '../network/http/http';
import { Action } from './action';
declare type Options = {
    ids: string[];
};
export declare class PostSign implements Action<Options> {
    execute(scope: Scope, options: Options, authData?: AuthData): Promise<void>;
    static onPutHook: (ids: string[], lanes: Lane[], authData?: AuthData) => Promise<void>;
}
export {};
