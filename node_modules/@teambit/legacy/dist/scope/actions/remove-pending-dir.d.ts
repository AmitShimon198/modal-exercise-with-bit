import { Scope } from '..';
import { Action } from './action';
declare type Options = {
    clientId: string;
};
export declare class RemovePendingDir implements Action<Options> {
    execute(scope: Scope, options: Options): Promise<void>;
}
export {};
