import { Scope } from '..';
import { BitId } from '../../bit-id';
import { Consumer } from '../../consumer';
import ModelComponent from '../models/model-component';
export declare type untagResult = {
    id: BitId;
    versions: string[];
    component?: ModelComponent;
};
export declare function removeLocalVersion(scope: Scope, id: BitId, version?: string, force?: boolean | undefined): Promise<untagResult>;
export declare function removeLocalVersionsForAllComponents(consumer: Consumer, version?: string, force?: boolean): Promise<untagResult[]>;
export declare function removeLocalVersionsForComponentsMatchedByWildcard(consumer: Consumer, version?: string, force?: boolean, idWithWildcard?: string): Promise<untagResult[]>;
