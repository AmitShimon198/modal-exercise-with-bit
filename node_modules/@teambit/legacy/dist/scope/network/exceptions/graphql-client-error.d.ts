import { BitError } from '@teambit/bit-error';
import { ClientError } from 'graphql-request';
export declare class GraphQLClientError extends BitError {
    private err;
    private url;
    private scopeName;
    constructor(err: ClientError, url: string, scopeName: string);
    report(): string;
    getRemoteInfo(): string;
}
