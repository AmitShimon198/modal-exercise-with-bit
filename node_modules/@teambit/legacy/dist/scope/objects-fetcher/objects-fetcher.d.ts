import { BitId } from '@teambit/legacy-bit-id';
import { Scope } from '..';
import { FETCH_OPTIONS } from '../../api/scope/lib/fetch';
import { Remotes } from '../../remotes';
import { Repository } from '../objects';
export declare class ObjectFetcher {
    private repo;
    private scope;
    private remotes;
    private fetchOptions;
    private ids;
    private context;
    private failedScopes;
    constructor(repo: Repository, scope: Scope, remotes: Remotes, fetchOptions: Partial<FETCH_OPTIONS>, ids: BitId[], context?: {});
    fetchFromRemoteAndWrite(): Promise<void>;
    private fetchFromSingleRemote;
    private writeFromSingleRemote;
    private showProgress;
}
