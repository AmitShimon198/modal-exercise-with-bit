import { TimerResponse } from './response';
export declare class Timer {
    private startTime;
    start(): Timer;
    stop(): TimerResponse;
    private calculateElapsed;
    static create(): Timer;
}
