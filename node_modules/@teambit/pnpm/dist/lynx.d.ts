import { ResolvedPackageVersion, Registries, PackageManagerProxyConfig } from '@teambit/dependency-resolver';
import { Logger } from '@teambit/logger';
export declare function install(rootPathToManifest: any, pathsToManifests: any, storeDir: string, registries: Registries, proxyConfig?: PackageManagerProxyConfig, logger?: Logger): Promise<void>;
export declare function resolveRemoteVersion(packageName: string, rootDir: string, storeDir: string, registries: Registries, proxyConfig?: PackageManagerProxyConfig): Promise<ResolvedPackageVersion>;
