/// <reference types="express" />
import { Request, Response, Route } from '@teambit/express';
import type { Logger } from '@teambit/logger';
import { PreviewMain } from './preview.main.runtime';
declare type UrlParams = {
    previewPath?: string;
};
export declare class PreviewRoute implements Route {
    private preview;
    private logger;
    constructor(preview: PreviewMain, logger: Logger);
    route: string;
    method: string;
    middlewares: ((req: Request<UrlParams>, res: Response) => Promise<import("express").Response<any>>)[];
}
export {};
