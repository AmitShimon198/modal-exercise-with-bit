import { BuildContext } from '@teambit/builder';
import { Target, BundlerResult, BundlerContext } from '@teambit/bundler';
import { BundlingStrategy } from '../bundling-strategy';
import { PreviewDefinition } from '../preview-definition';
import { PreviewTask } from '../preview.task';
export declare class ComponentBundlingStrategy implements BundlingStrategy {
    name: string;
    computeTargets(context: BuildContext, previewDefs: PreviewDefinition[], previewTask: PreviewTask): Promise<Target[]>;
    computeResults(context: BundlerContext, results: BundlerResult[], previewTask: PreviewTask): Promise<{
        componentsResults: {
            errors: Error[];
            component: import("@teambit/component").Component;
            warning: string[];
        }[];
        artifacts: {
            name: string;
            globPatterns: string[];
        }[];
    }>;
}
