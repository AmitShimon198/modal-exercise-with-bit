import { Bundler, DevServer } from '@teambit/bundler';
import { DeployContext } from '@teambit/application';
export declare type ReactAppOptions = {
    name: string;
    entry: string[];
    ssr?: boolean;
    bundler?: Bundler;
    devServer?: DevServer;
    deploy?: (context: DeployContext) => Promise<void>;
    portRange?: number[];
};
