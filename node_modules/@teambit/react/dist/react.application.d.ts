import { Capsule } from '@teambit/isolator';
import { Application, AppContext, DeployContext } from '@teambit/application';
import { BuildContext } from '@teambit/builder';
import { ReactEnv } from './react.env';
export declare class ReactApp implements Application {
    readonly name: string;
    readonly entry: string[];
    readonly portRange: number[];
    private reactEnv;
    private rootPath;
    readonly deploy?: ((context: DeployContext) => Promise<void>) | undefined;
    constructor(name: string, entry: string[], portRange: number[], reactEnv: ReactEnv, rootPath: string, deploy?: ((context: DeployContext) => Promise<void>) | undefined);
    applicationType: string;
    run(context: AppContext): Promise<number>;
    build(context: BuildContext, aspectId: string, appCapsule: Capsule): Promise<DeployContext>;
    private getDevServerContext;
}
