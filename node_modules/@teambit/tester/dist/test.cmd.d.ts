import { Command, CommandOptions } from '@teambit/cli';
import { Logger } from '@teambit/logger';
import { Workspace } from '@teambit/workspace';
import type { TesterMain } from './tester.main.runtime';
declare type TestFlags = {
    watch: boolean;
    debug: boolean;
    env?: string;
    scope?: string;
    junit?: string;
    coverage?: boolean;
};
export declare class TestCmd implements Command {
    private tester;
    private workspace;
    private logger;
    name: string;
    description: string;
    alias: string;
    group: string;
    shortDescription: string;
    options: CommandOptions;
    constructor(tester: TesterMain, workspace: Workspace, logger: Logger);
    render([userPattern]: [string], { watch, debug, env, scope, junit, coverage }: TestFlags): Promise<JSX.Element | {
        code: number;
        data: JSX.Element;
    }>;
}
export {};
