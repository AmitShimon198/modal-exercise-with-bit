import { Bundler, BundlerResult, Target } from '@teambit/bundler';
import { Logger } from '@teambit/logger';
import { Configuration } from 'webpack';
export declare class WebpackBundler implements Bundler {
    private targets;
    private configs;
    private logger;
    private webpack;
    constructor(targets: Target[], configs: Configuration[], logger: Logger, webpack: any);
    run(): Promise<BundlerResult[]>;
    private getComponents;
}
