import { Component, ComponentID, AspectData } from '@teambit/component';
export declare type SerializableResults = {
    results: any;
    toString: () => string;
};
export declare type OnComponentChange = (component: Component) => Promise<SerializableResults>;
export declare type OnComponentAdd = (component: Component) => Promise<SerializableResults>;
export declare type OnComponentRemove = (componentId: ComponentID) => Promise<SerializableResults>;
export declare type OnComponentEventResult = {
    extensionId: string;
    results: SerializableResults;
};
export declare type OnComponentLoad = (component: Component) => Promise<AspectData>;
