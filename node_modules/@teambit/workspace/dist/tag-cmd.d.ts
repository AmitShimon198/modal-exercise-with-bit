import { Command, CommandOptions } from '@teambit/cli';
import { BasicTagParams } from '@teambit/legacy/dist/api/consumer/lib/tag';
export declare class Tag implements Command {
    name: string;
    group: string;
    shortDescription: string;
    description: string;
    alias: string;
    loader: boolean;
    options: CommandOptions;
    migration: boolean;
    remoteOp: boolean;
    report([id]: [string[]], { message, ver, all, snapped, patch, minor, major, preRelease, force, verbose, ignoreUnresolvedDependencies, ignoreIssues, ignoreNewestVersion, skipTests, skipAutoTag, scope, build, soft, persist, disableDeployPipeline, disableTagPipeline, forceDeploy, incrementBy, }: {
        all?: boolean | string;
        snapped?: boolean | string;
        ver?: string;
        patch?: boolean;
        minor?: boolean;
        major?: boolean;
        ignoreUnresolvedDependencies?: boolean;
        ignoreIssues?: boolean;
        scope?: string | boolean;
        incrementBy?: number;
        disableDeployPipeline?: boolean;
        disableTagPipeline?: boolean;
    } & Partial<BasicTagParams>): Promise<string>;
}
